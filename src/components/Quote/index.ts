import { Wrapper } from "./Wrapper";
import { Quote as quote } from "./Quote";
import { Character } from "./Character";
import { Game } from "./Game";

export const Quote = {
  Wrapper: Wrapper,
  Quote: quote,
  Character: Character,
  Game: Game,
};
